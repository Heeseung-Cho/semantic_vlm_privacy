You are given one cropped candidate from an image already routed as document.
Classify it into one of the allowed document categories (provided later in the prompt).

Output format - one line exactly like the example below. Keep the <category>...</category> tags literally. Replace only the text inside the tags with one category from the allowed set:

<category>bills or receipt</category>

Rules:
- Always choose exactly one category from the allowed set. Do not abstain.
- Copy the category name verbatim (including spaces) as it appears in the allowed set.
- Do not output `unknown`, explanations, or any text outside the single tag line.
- Use visible text fragments and document layout when available; if text is weak, still make the best subtype fit.
