You are given one query image.
Identify likely private-object categories, decide whether text/OCR-style document handling is needed, and produce detector-friendly prompts.

Return only the following XML-like fields:
<route_type>one route chosen from: {{route_list}}, or none</route_type>
<categories>comma-separated likely categories chosen from: {{category_list}}, or none</categories>
<cue>comma-separated detector prompts from coarse to specific</cue>
<null>yes or no</null>

Route guidance:
{{route_descriptions}}

Category guidance:
{{category_descriptions}}

Rules:
- Use route_type=document when the visible target is a paper-like document, newspaper, form, report, statement, receipt, letter, transcript, or prescription whose subtype may require reading visible text or layout.
- Use route_type=non document for physical objects such as cards, boxed products, plastic test devices, condom packaging/wrappers, pill bottles, or tattoo sleeves.
- Choose all visible private-object categories that likely appear in the image, usually 1 to 3 category names copied exactly from the allowed category set.
- If both document and non-document private objects are visible, include categories for both and set route_type=document only when at least one document category is a plausible target.
- Keep <cue> short, usually 2 to 5 detector-friendly noun phrases.
- For document route cues, include category-specific document nouns such as receipt, bank statement, letter with address, transcript, mortgage report, medical record, prescription form, newspaper.
- For non-document route cues, include object nouns such as credit card, business card, pregnancy test, pregnancy test box, condom box, condom wrapper, pill bottle, tattoo sleeve.
- Avoid generic fallback cues when a narrower category-specific cue fits the image.
- If no likely private target appears in the image, output <route_type>none</route_type>, <categories>none</categories>, <cue>empty</cue>, and <null>yes</null>.
- Do not output any extra text.
