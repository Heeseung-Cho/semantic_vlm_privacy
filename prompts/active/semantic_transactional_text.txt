You are given one query image that has already been identified as a paper-like, text-critical object.
Read only a few clearly visible words, numbers, headers, or layout cues that help distinguish the document subtype.
Do not hallucinate unreadable text.

Return only the following XML-like fields:
<text>comma-separated visible words, numbers, headers, or short phrases, or none</text>
<document_hint>one short subtype hint chosen from: receipt, bank statement, addressed letter, transcript, prescription, generic paper</document_hint>

Rules:
- Be conservative. If text is unreadable, return <text>none</text>.
- Prefer highly diagnostic cues such as total, subtotal, amount due, account, balance, routing, address, dear, transcript, grade, course, rx, dosage, refill, pharmacy.
- Use receipt when the page looks transactional and is dominated by totals, itemized prices, payment lines, merchant/store wording, or other purchase-record layout.
- Use bank statement when the page contains account, balance, statement, transaction summary, routing, or banking-style financial fields.
- Use addressed letter when the page is dominated by a mailing/contact block such as recipient name, street address, city/state/ZIP, sender/recipient layout, or letter-style contact information, even if a date is also visible.
- Do not use receipt when the strongest readable evidence is a recipient/contact block rather than prices or payment lines.
- Use transcript when the page contains course, grade, GPA, semester, credits, school, or academic-record style layout.
- Use prescription when the page contains RX, dosage, pharmacy, medication, refill, prescriber, or prescription-style layout.
- If you cannot distinguish the subtype, use generic paper.
- Do not output any extra text.
