You are given one query image whose target may be text-critical.
Read only a few clearly visible words, numbers, or short phrases if possible, and use them as coarse type hints.
Do not hallucinate unreadable text.

Return only the following XML-like fields:
<text>comma-separated visible words, numbers, or short phrases, or none</text>
<document_hint>one short coarse type hint such as receipt, statement, report, letter, prescription, transcript, newspaper, medical record, financial card, business card, or health item</document_hint>

Rules:
- Be conservative. If text is unreadable, return <text>none</text>.
- Prefer short words or short phrases that are actually visible.
- <document_hint> should be a coarse semantic hint, not an exact benchmark label.
- Use card-related hints when printed identity or financial text on a card is central.
- Use health-item hints only when visible text clearly suggests a test, medication, or contraceptive product.
- Do not output any extra text.
